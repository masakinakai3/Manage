<!--
  Copyright (c) 2026 Masaki Nakai (https://github.com/masakinakai3)
  Released under the MIT license
  https://opensource.org/licenses/mit-license.php
-->
# ユーザーマニュアル

## 1. はじめに

Resource Manager は、テーマ別の配賦計画とメンバー別の負荷確認を行うためのツールです。  
本書では、日常利用で必要になる主要操作を説明します。
（※ ユーザー追加などの管理者向け機能は本マニュアルの対象外です。）

## 2. 起動方法

### EXE 版

1. `manage_app.exe` を起動します。
2. ブラウザが自動的に開き、アプリ画面が表示されます。
3. ログイン画面が表示された場合は、ユーザー名とパスワードを入力してログインします。  
   （初回起動時は `admin` ユーザーが自動作成されます。パスワードは管理者に確認してください。）

### 開発版

1. Flask サーバーを起動します。
2. 必要に応じて Vite 開発サーバーを起動します。
3. ブラウザで画面を開きます。
4. ローカル（localhost）からのアクセスは自動ログインされます。

## 3. 画面構成

左側のサイドバーから各画面を切り替えます。

- ガントチャート
- メンバー負荷
- インサイト
- テーマ一覧
- メンバー一覧

サイドバー下部では次の操作もできます。

- テーマ切替
- ショートカットヘルプ表示
- JSON エクスポート
- JSON インポート
- ログアウト

画面幅が1024px以下の場合、左上のメニューボタンでサイドバーを開きます。サイドバーは背景をクリックするか、`Escape`キーを押すか、移動先を選ぶと閉じます。ページをスクロールしてもメニューボタンは左上に表示されます。

## 4. 初回利用

初回はオンボーディングパネルが表示されます。

- `サンプルデータ作成` を押すと、テーマ、メンバー、配賦の例が自動投入されます
- `閉じる` を押すとオンボーディングを非表示にします

## 5. ガントチャートの使い方

### 5.1 表示期間の変更

- `1M / 3M / 6M / 1Y` で表示スケールを切り替えます
- `前へ / 次へ / 現在へ` で期間を移動します
- 期間プリセットで表示基準を変更できます
  - 直近6か月
  - 直近12か月
  - 直近24か月
  - 現在四半期
  - 今年
- `標準表示 / コンパクト表示` で月列の密度を切り替えます

### 5.2 絞り込み

以下の条件で表示対象を絞り込めます。

- テーマ名
- カテゴリ
- 担当メンバー
- ステータス
- 優先度

`絞り込み解除` で初期状態に戻せます。

### 5.3 グルーピング

`グループ化` で次の単位にまとめられます。

- なし
- カテゴリ
- ステータス
- 優先度
- 開発ランク

### 5.4 テーマ行の操作

テーマ行では次を操作できます。

- 展開 / 折りたたみ
- ステータス変更
- マイルストーン編集
- メンバー追加（一括選択で複数メンバーを同時に割り当て可能）

見出し横の展開／折りたたみアイコンで、すべてのテーマをまとめて展開または折りたためます。データ読込中に折りたたみを選んだ場合も、読込完了後の行へ反映されます。

上部のコントロール画面は初期状態では閉じています。`コントロールを開く`で表示し、`コントロールを閉じる`で再び閉じられます。手動で開閉した状態は次回表示でも使用されます。

画面幅が720px以下の場合、ガント表の前にテーマナビが表示されます。ウィンドウ幅を変更した場合も自動で表示が切り替わり、テーマを選ぶと対象行へ移動できます。完了・中止テーマは非アクティブな色で表示されます。

### 5.5 配賦セルの編集

#### クリック編集

1. 編集したいセルをクリックします。
2. 通常幅ではインラインエディタ、1024px以下では表の下に表示される詳細編集で値を編集します。
3. 保存します。

編集中は一つの編集面だけが表示され、同じセルのインライン編集と詳細編集が競合しません。

#### キーボード編集

- 矢印キーでセル移動
- 数字キーで直接入力開始
- `Enter` で保存
- `Delete` または `Clear` で配賦を未入力（`null`）に戻す
- 数値の `0` を保存した場合は、明示的な `0%` として保持する

#### 一括反映

詳細パネルでは、選択セルの月以降に同じ値を適用するプレビューができます。

### 5.6 ドラッグアンドドロップ

- 同一テーマ内で、メンバーや月をまたいで配賦を移動できます
- 移動後は Undo / Redo で戻したりやり直したりできます

### 5.7 コピー / ペースト

- 複数月のセル範囲を選択してコピーできます
- 貼り付け先の月から同じ並びで配賦を複製できます

### 5.8 マイルストーン

テーマのマイルストーン編集画面では次を設定できます。

- 月
- ラベル
- 完了状態

開発完了月の設定については、セクション 8.2（テーマ編集）を参照してください。

ガント上では対象月のテーマサマリ行に表示されます。

### 5.9 スナップショット

#### 保存

1. スナップショット名を入力します
2. 保存ボタンを押します

#### 比較

1. スナップショット一覧から比較対象を選びます
2. ガント上に差分が表示されます
3. 変更セル数、変更テーマ数、変更メンバー数がサマリに表示されます

### 5.10 保存ビュー

表示条件を保存して再利用できます。

#### 保存

1. 期間、スケール、絞り込み条件を整えます
2. `現在の表示を保存` を押します
3. 名前を入力して保存します

#### 適用

1. `表示プリセット` ドロップダウンから対象ビューを選びます
2. 画面が保存内容に切り替わります

#### 削除

1. 対象の保存ビューを選びます
2. `削除` を押します

### 5.11 CSV 出力

- `CSV出力` ボタンで現在表示中のガントデータを CSV で出力できます。
  （※XLSX形式での出力機能はAPIとして存在しますが、現在画面上のボタンは配置されていません。）

## 6. メンバー負荷画面の使い方

### 6.1 概要

メンバーごとの総負荷と、テーマ別の内訳を確認できます。

### 6.2 できること

- 表示期間の切り替え
- メンバー検索
- 内訳の展開 / 折りたたみ
- 負荷超過や余力の確認
- `次の過負荷へ` による警告セルへの移動
- 標準 / コンパクトの表密度切り替え
- テーマ内訳の CSV 出力
- 1M表示で月別キャパシティを設定・解除

### 6.3 画面の見方

- 上段サマリで平均負荷、超過メンバー数、余力あり人数、未配賦人数を確認できます
- メンバー行では総負荷が表示されます
- メンバー名の横の `通常` は、月別指定がない月に使うキャパシティです
- 1M表示では各月セルの `上限` ボタンから1〜200%を設定できます。空欄で保存すると月別指定を解除します
- 3M / 6M / 1Y表示の上限は対象期間の平均値です。月別編集は1M表示へ切り替えて行います
- 負荷セル内の `内訳` ボタンを選ぶと、マウス・キーボード・タッチのいずれでもテーマ別内訳を固定表示できます
- 内訳はポップアップの `閉じる` または `Escape` で閉じられます。旧来の上部詳細パネルは表示しません
- 展開するとテーマ別負荷が表示されます
- テーマ行ではマイルストーンや開発完了月も見られます
- メンバー見出し横の展開／折りたたみアイコンで、すべてのテーマ内訳をまとめて切り替えられます
- 上部の`コントロール画面を閉じる`で期間・検索・移動・CSV出力を隠し、`コントロール画面を開く`で再表示できます
- 画面幅が820px以下の場合、コントロール画面は初期状態で閉じています。表とサマリはコントロールを閉じたまま確認できます
- 負荷の凡例は`負荷の見方と操作`を選ぶと展開します

### 6.4 セル編集

- スケールが 1 か月表示のとき、テーマ行セルをクリックして配賦を編集できます

## 7. インサイト画面の使い方

### 7.1 概要

インサイト画面は、計画全体の状況を可視化し、新規案件追加のシミュレーションや負荷の横断的確認を行うための画面です。

### 7.2 確認できる内容

- テーマ数、平均負荷、警告セル数、配賦中メンバー数
- カテゴリ別分布
- ステータス別分布
- 部門別平均負荷
- 新規案件追加シミュレーション（シナリオ提案）
- 月次推移
- Project Load Ribbon
- 負荷上位テーマ

### 7.3 典型的な使い方

1. ガントで計画を更新します
2. インサイト画面に切り替えます
3. ダッシュボードやリボンで部門・テーマ別の偏りを確認します
4. 新規案件が入る場合はシミュレーションで候補を探索します

Project Load Ribbonの月ボタンを選ぶと、テーマ別負荷・総容量・担当内訳を表で確認できます。欠損値は`0%`へ置き換えず`データなし`と表示します。通信に失敗した場合は再試行し、サイドバーのデータ状態が`取得失敗`または`オフライン`から回復したことを確認してください。

## 8. テーマ一覧の使い方

### 8.1 一覧操作

- テーマ名 / カテゴリで検索
- 並び替え
  - 名前順
  - 優先度順
  - ステータス順
  - メンバー数順

### 8.2 新規作成 / 編集

テーマ編集では次を設定できます。

基本項目を先に入力し、開発完了月・マイルストーン・色は`計画マーカーと表示色`を開いて設定します。

- テーマ名
- カテゴリ
- ステータス
- 優先度
- 開発ランク
- 開発完了月（複数設定可能、各完了月に完了状態を持たせられます）
- マイルストーン
- 色
- 開始月 / 終了月
- 割り当てメンバー（一括選択可能）

### 8.3 削除

- 削除すると関連データも影響を受けるため、必要に応じて先に JSON エクスポートを行ってください

## 9. メンバー一覧の使い方

### 9.1 一覧操作

- 表示名 / 部門で検索
- 並び替え
  - 名前順
  - キャパシティ順
  - 部門順
  - 有効状態順

### 9.2 新規作成 / 編集

メンバー編集では次を設定できます。

- 表示名
- 部門
- 通常月のキャパシティ（既定100 = フル稼働。月別指定がない月に適用されます）
- 有効 / 無効

### 9.3 削除

- 削除前にバックアップを取る運用を推奨します

## 10. バックアップと復元

### 10.1 JSON エクスポート

1. サイドバーの `JSON エクスポート` を押します
2. 全データが JSON ファイルとして保存されます

ユーザーアカウントもバックアップされます。パスワードは平文ではなくハッシュとして保存されますが、バックアップファイルは認証情報を含む機密データとして安全に保管してください。

### 10.2 JSON インポート

1. サイドバーの `JSON インポート` を押します
2. バックアップファイルを選択します
3. 実行します

注意:

- インポートは既存データを置き換えます
- 新しい形式のバックアップにユーザーが含まれる場合、ユーザーアカウントも置き換わり、完了後にログアウトします。復元されたアカウントで再ログインしてください
- 旧形式のバックアップにユーザーが含まれない場合、現在のユーザーアカウントは変更されません
- 実行前に必要なバックアップを取得してください

## 11. キーボードショートカット

- `?`: ショートカットヘルプ表示
- `/`: 現在画面の検索欄へフォーカス
- `g`: ガント画面
- `l`: メンバー負荷画面
- `i`: インサイト画面
- `t`: テーマ一覧
- `u`: メンバー一覧
- `Ctrl + Z`: Undo
- `Ctrl + Shift + Z`: Redo
- `Ctrl + Y`: Redo（`Ctrl + Shift + Z` の代替）

## 12. トラブルシューティング

### 12.1 画面が最新にならない

- 画面再読み込みを試してください
- 保存ビューや期間設定が残っていないか確認してください

### 12.2 負荷警告が出る

- メンバーのキャパシティを超える月次配賦があります
- ガントまたはメンバー負荷画面で内訳を確認し、必要なら1M表示の上限ボタンから月別キャパシティを調整してください

### 12.3 インポートで失敗する

- JSON バックアップ形式で出力したファイルか確認してください
- 手編集した JSON では必須キー不足で失敗することがあります

### 12.4 EXE を作り直したい

- 通常再ビルド: `.\.venv\Scripts\python.exe build_exe.py`
- 強制再ビルド: `.\.venv\Scripts\python.exe build_exe.py --force`
- クリーンビルド: `.\.venv\Scripts\python.exe build_exe.py --clean`
