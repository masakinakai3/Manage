# リソース管理ツール仕様書（改訂版：確定仕様反映）

## 1. 目的 / ゴール

*   開発テーマ（案件）ごとの人員配置（誰が・いつ・何割）を可視化し、リソース計画の調整を支援する。
*   可視化のための入力・更新工数を最小化し、運用が継続できる設計とする。

***

## 2. 対象範囲 / 非対象範囲

### 2.1 対象範囲

*   テーマ管理（作成・一覧表示）
*   テーマ × メンバー × 月単位の割当（負荷率）登録・編集
*   テーマ視点ガント表示（**メンバー別表示必須、折りたたみ可**）
*   メンバー視点の月次負荷集計（複数テーマの合算）
*   ドラッグ&ドロップによる割当編集（期間移動・伸縮・率変更）
*   100%超過の可視化（警告表示）

### 2.2 非対象範囲

*   工程管理、進捗管理、タスク管理
*   実績工数、勤怠連携、原価・予算管理

***

## 3. 取り扱うデータ規模（上限）

*   開発テーマ：最大 50
*   開発メンバー：最大 50
*   期間：最大 5年（60か月）
*   最小単位：1か月（暦月、YYYY-MM）

***

## 4. 用語定義

*   **開発テーマ**：リソースを割当てる計画単位
*   **メンバー**：割当て対象の個人
*   **割当（Allocation）**：テーマに対するメンバーの月単位配賦
*   **負荷率（割当率）**：月あたりの投入割合（%）
*   **容量（Capacity）**：メンバーが月に投入可能な総量（既定 100%）

***

## 5. 入力制約（確定）

### 5.1 負荷率（割当率）の刻み（確定）

*   負荷率は **0%〜100%**
*   入力の最小刻みは **1%刻み**（整数）

### 5.2 100%超過の扱い（確定）

*   メンバーの月次合計負荷率が **100%を超えることは可能（入力許容）**
*   ただし、**明確な警告表示**で超過を可視化する（詳細は 9 章）

***

## 6. 権限（最小構成）

*   **管理者（Admin）**
    *   メンバー・テーマの追加/無効化
    *   全割当の編集、表示設定、エクスポート（任意）
*   **一般利用者（User）**
    *   閲覧
    *   編集可否は運用で選択（初期は「閲覧のみ」推奨も可）

***

## 7. データモデル（論理）

### 7.1 エンティティ

*   Member
    *   member\_id（PK）
    *   表示名
    *   所属（任意）
    *   capacity（%：既定100、任意で変更可能）
    *   有効フラグ（退職等は無効化）
*   Theme
    *   theme\_id（PK）
    *   名称
    *   区分/カテゴリ（任意）
    *   ステータス（計画中/進行中/完了/保留：任意）
*   Allocation
    *   theme\_id（FK）
    *   member\_id（FK）
    *   month（YYYY-MM）
    *   allocation\_rate（0〜100、1%刻み）
    *   メモ（任意）
    *   最終更新者/更新日時（監査：任意）

### 7.2 一意制約

*   Allocation は (theme\_id, member\_id, month) を一意とする
    *   重複登録はせず、更新で上書きする

***

## 8. 画面 / 機能要件（詳細）

## 8.1 テーマ一覧（テーマ視点ガント） ※確定反映：メンバー別必須

### 8.1.1 一覧表示

*   開発テーマを一覧表示する
*   主要項目：
    *   テーマ名
    *   期間（割当の最小月〜最大月）
    *   担当人数（当該期間内で1%以上割当があるメンバー数）

### 8.1.2 ガントチャート表示（テーマ視点）

- テーマ行は折りたたみ可能な階層表示とする。

【表示仕様（必須）】
(1) 上段：テーマ合算行（サマリ行）
- テーマ全体の月別負荷を%で表示する。
  - 定義：theme_month_load(%) = Σ allocation_rate(theme, member, month)
- 表示ラベル例：250%（小数は扱わない。集計結果は整数%で表示する）
- 0% は空白表示、1%以上は%表示とする。

(2) 下段：メンバー別行（詳細行）
- テーマ配下に、割当が存在するメンバー行を表示する（0%のみのメンバーは表示しない）。
- 各メンバー行は月別セルに allocation_rate（0〜100%）を表示する。

【折りたたみ仕様（必須）】
- 折りたたみ時：テーマ合算行のみ表示する。
- 展開時：テーマ合算行 + メンバー別行を表示する。
- 初期状態（折りたたみ/展開）は設定で選択可能とする（推奨：折りたたみ）。

***

## 8.2 メンバー負荷（メンバー視点）

### 8.2.1 月次負荷の集計表示

*   メンバーごとに月別合計負荷率（Σテーマ割当率）を表示
*   capacity（既定100%）に対して
    *   超過（>100%）は警告表示（9章）
    *   余力（<100%）は任意で表示（例：余力 20%）

### 8.2.2 内訳表示

*   メンバー×月セルを選択すると、当該月のテーマ別内訳を表示
    *   テーマ名 / 割当率 / 合計

***

## 8.3 表示スケール切替

*   1か月 / 3か月 / 半年 / 年 を切替可能
*   **データの最小粒度は常に月**（集約は表示のみ）
*   集約表示のルール（推奨）
    *   3か月/半年/年は「平均負荷%」表示（または「人月合計」表示を選択可能）
    *   初期は平均負荷%を採用（直感性重視）

***

## 8.4 編集（ドラッグ&ドロップ）

### 8.4.1 操作対象（必須）

*   バーのドラッグ：期間を移動
*   端点ドラッグ：期間を伸縮
*   クリック編集：割当率（1%刻み）変更

### 8.4.2 操作結果（必須）

*   D\&D確定で Allocation を月単位に展開して保存
    *   例：2026-01〜2026-03 を 50% → 3レコード更新
*   同一 (theme, member, month) が存在する場合は上書き

### 8.4.3 0%の扱い（推奨）

*   0%になった月は Allocation レコードを削除する（データ量と表示の簡素化）

### 8.4.4 取り消し（任意）

*   直前1回の Undo（実運用で有効）

***

## 9. 警告表示仕様（確定：100%超過は可能、警告で可視化）

9.1 警告対象
- メンバー月次合計負荷率が capacity を超過した場合に警告表示する。
  - 判定：member_month_load(%) > capacity(%)

9.2 警告の強さ（確定）
- 超過は「警告のみ」とし、入力・保存・表示を制限しない。
- 強警告（段階的な警告レベル）や入力ブロックは行わない。

9.3 警告表示の要件（必須）
- どのメンバーが、何月に、何%超過しているかが即座に分かること。
- 視認性を確保するため、色・アイコン・強調表示のいずれか（または併用）で表現する。
- 超過量（例：+20%）が確認できること（ツールチップ/詳細表示など）。

9.4 表示例（必須要件を満たす一例）
- メンバー負荷画面：
  - 超過セルを警告色でハイライトし、ツールチップに「合計120%（+20%超過）」を表示する。
- テーマ視点ガント（メンバー別行）：
  - 当該メンバー×月セルに警告アイコン（または枠線強調）を表示する。
  - クリック/ホバーで当該月の内訳（テーマ別割当）を参照できる導線を設けてもよい（任意）。

***

## 10. 計算仕様（確定反映）

### 10.1 メンバー月次負荷

*   `member_month_load(%) = Σ allocation_rate(theme, member, month)`
*   超過判定：
    *   `member_month_load > capacity` → 警告

### 10.2 テーマ月次負荷（サマリ行用）

*   `theme_month_load(%) = Σ allocation_rate(theme, member, month)`
*   人月換算（表示に採用する場合）：
    *   `theme_month_mm = theme_month_load / 100`

***

## 11. 非機能要件（抜粋）

*   50テーマ×50人×60か月規模でガント表示が実用速度であること
*   D\&D 操作はストレスなく追従し、保存失敗時は再試行導線があること

***

## 12. 受入基準（確定仕様に合わせて更新）

*   負荷率は **1%刻み**で入力できる（例：33%が設定可能）
*   メンバー月合計が **100%を超えても登録できる**
*   100%超過時に **警告表示**が出て、超過量が分かる
*   テーマ視点ガントにおいて
    *   テーマの \*\*合算行（サマリ）\*\*が表示される
    *   テーマを展開すると **メンバー別行が必ず表示される**
    *   折りたたみ/展開ができる
*   メンバーが複数テーマを担当した際、月次合計負荷が正しく集計される
*   D\&Dで期間移動・伸縮・率変更ができ、月単位で保存される

***