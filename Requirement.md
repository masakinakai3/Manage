<!--
  Copyright (c) 2026 Masaki Nakai (https://github.com/masakinakai3)
  Released under the MIT license
  https://opensource.org/licenses/mit-license.php
-->
# 要件定義書 (Requirement.md)

## 1. 概要

本システムは、開発プロジェクトにおけるテーマごとの人員配置（リソースアサイン）を管理・可視化するためのツールである。
Excel等での管理に代わり、Webブラウザ上で直感的に操作でき、リアルタイムに負荷状況を把握できることを目的とする。

## 2. システム構成

- **形態**: Webアプリケーション（シングルページアプリケーション）
- **配布**: Windows向け単一実行ファイル (.exe) または Webサーバー配備
- **認証**: なし（起動時は自動的に管理者権限で動作）

## 3. 機能要件

### 3.1 ガントチャート画面 (メインビュー)

#### 表示機能

- **マトリクス表示**: 行に「テーマ」、列に「月」を配置したガントチャート形式。
- **階層構造**: テーマ行の下に、アサインされたメンバー行を展開して表示する。
- **期間表示**: テーマの開始月〜終了月の範囲をセル背景色等でハイライト表示する。
- **ステータス表示**: テーマの進行状態（計画中/実行中/完了など）とカテゴリをバッジで表示する。
- **集計表示**:
  - テーマ行（折りたたみ時）のセルには、その月の合計稼働率を表示する。
  - ホバー時、メンバーごとの負荷内訳をツールチップで表示する。
- **スケール切替**: 表示期間の粒度を 1ヶ月 / 3ヶ月 / 6ヶ月 / 1年 で切り替え可能。

#### 操作機能

- **展開/折りたたみ**: テーマごとにメンバー行の表示/非表示を切り替え可能。状態は保存される。
- **メンバーアサイン**: テーマ行の「＋」ボタンから、メンバーを選択して追加できる。
- **アサイン解除**: メンバー行の「×」ボタンから、アサインを解除できる。
- **稼働率入力**: セルをクリックして、その月の稼働率(%)を数値入力できる。
- **ドラッグ&ドロップ**:
  - **期間移動**: 同じメンバーのバーをドラッグして、別の月に移動できる。
  - **メンバー変更**: バーをドラッグして、別のメンバー行に移動できる。

### 3.2 メンバー負荷表画面

- **メンバー別集計**: 行に「メンバー」、列に「月」を配置し、全テーマの合計負荷率を表示する。
- **キャパシティ管理**: メンバーごとに設定されたキャパシティ（標準100%）と比較。
- **アラート**: 合計負荷率がキャパシティを超過した場合、赤色等で警告表示する。

### 3.3 管理機能

#### テーマ管理

- テーマの新規作成、編集、削除。
- 設定項目: テーマ名、カテゴリ、ステータス、開始月・終了月（YYYY-MM形式）、テーマカラー。
- 期間入力はスピンボタン（年・月独立）により操作性を向上。

#### メンバー管理

- メンバーの新規作成、編集、削除。
- 設定項目: 氏名、所属、ロール、標準キャパシティ、スキルセット。

### 3.4 システム機能

- **CSVエクスポート**: 表示中のガントチャートデータをCSV形式でダウンロード可能。
- **データ永続化**: 入力データはローカルのSQLiteデータベースファイル (`database.db`) に保存される。
- **状態保持**: 画面リロード時も、ガントチャートの開閉状態などが維持される。

## 4. 非機能要件

- **レスポンス**: 操作（入力、移動）に対して即座に画面反映されること。
- **スタンドアロン動作**: 外部インターネット接続を必須とせず、ローカル環境で完結して動作すること。
- **インストール不要**: EXEファイルを配置するだけで動作すること（Python/Node.js環境不要）。
